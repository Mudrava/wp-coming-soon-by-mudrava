/**
 * Countdown Block – Save Component
 *
 * Returns null because this is a dynamic block rendered server-side
 * via PHP render_callback. The view script hydrates the server-
 * rendered HTML into a live countdown on the frontend.
 *
 * @package Mudrava\ComingSoon
 * @since   1.0.0
 */

export default function Save() {
    return null;
}
